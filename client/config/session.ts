export const sessionConfig = {
    timeoutMinutes: 15,
    warningMinutes: 1, // Optional: for showing a warning before timeout
    events: ['mousemove', 'mousedown', 'keypress', 'scroll', 'touchstart']
}
